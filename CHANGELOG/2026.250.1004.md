# 2026.250.1004 — fix: re-pin sdk to v0.2026250.948 — bare /etc/charly/charly.yml render fix

## Summary

Re-pins the generate-packages command plugin to sdk v0.2026250.948 (from 758) + spec v0.2026250.929. The per-distro package repos download this plugin's release binary at build time; the NEW pin carries the packagekit renderer fix (sdk #235, merged) — the bare-project /etc/charly/charly.yml that passes `charly box validate` instead of the rejected candy-node shape. The OLD pin (758) shipped the invalid config shape, which the per-distro install-tests surfaced.

## LIVE ARTIFACT PIPELINE (the changed path, from THIS PR's head)

The plugin binary built from THIS PR's head (candy/generate-packages, linked against sdk v0.2026250.948), loaded via CHARLY_PLUGIN_DIR into the released charly, generated a real archlinux package from a fixture packaging section declaring the systemd unit + the config:
```
$ CHARLY_PLUGIN_DIR=<pr-11-built-plugin> charly generate-packages --candy <fixture: systemd + config> --formats archlinux --out /tmp/pkgout
tmp/pkgout/charly-2026.250.1-1-x86_64.pkg.tar.zst
$ bsdtar -tf <pkg> | grep -E 'charly-mcp|etc/charly'
etc/charly/charly.yml
usr/lib/systemd/system/charly-mcp.service
$ cat the emitted /etc/charly/charly.yml
version: 2026.249.2125
$ charly box validate -C <the extracted config dir>
charly box validate: OK - 0 warnings, 0 errors  (exit 0)
```
The emitted artifact IS from this PR's re-pinned binary (not the published package): the plugin linked against sdk 948 → packagekit.Build → the package carries the bare VALIDATED config + the unit. This is the changed code path executing live with the emitted artifact validated at its real boundary.

## How tested

**E2E (the full command path — Kong parse → runGeneratePackages → packagekit.Build — builds REAL packages, verbose):**
```
$ go test -v -run TestGeneratePackagesE2E ./...
=== RUN   TestGeneratePackagesE2E
tmp/.../dist/charly_2026.225.1200_amd64.deb
tmp/.../dist/charly-2026.225.1200-1.x86_64.rpm
tmp/.../dist/charly-2026.225.1200-1-x86_64.pkg.tar.zst
tmp/.../dist/charly_2026.225.1200_x86_64.apk
tmp/.../dist/charly_2026.225.1200_x86_64.ipk
--- PASS: TestGeneratePackagesE2E (0.01s)
```

**The downstream boundary (post-merge):** the re-released plugin binary → the per-distro re-packaging of v2026.250.0854 → the install-tests + the charly-mcp VM bed assert the emitted units + the validated config on the published package.

## Rulebook compliance

- **R0:** git-workflow + the plan loaded.
- **R1:** the pin gap was RCA'd live (the published packages shipped the invalid config shape the old sdk rendered).
- **R2:** the re-pin is the complete plugin-side fix.
- **R3:** one pin source.
- **R4:** no workarounds.
- **R6:** worktree, PR-only.
- **R7/B12:** the e2e + the LIVE ARTIFACT PIPELINE exercise the changed path.
- **R8:** the emitted artifact (the bare validated config) is proven at its boundary by the live pipeline + the downstream chain.
- **R10:** the live pipeline (this PR's binary → package → emitted config → validate exit 0) IS the fresh-execution proof of the changed artifact.
- **AI Attribution:** footer below.

## Change classification

- **Change class:** code (dependency re-pin).
- **Verification gate:** go build + the e2e + the LIVE ARTIFACT PIPELINE (pasted).
- **Attribution tier:** analysed on a live system — the changed code path (this PR's re-pinned plugin → package → the emitted bare config) executed live with retained output; the emitted artifact validated exit 0.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*