# 2026.230.0722

Follow-ups from the validation of #3, plus the deprecated-runner bump that validation
surfaced. Batched rather than landed as three solo ceremonies.

## The tag-contract section says which repo it means

#3's section used `candy/generate-packages/` for **two different directories in two
different repositories** — this repo's Go module and the superproject's re-export shim — in
adjacent sentences, and never named "the superproject". A cold reader reconstructed the
correct tag and require line anyway, but reported low confidence about which repo the tag
belonged to. In a page whose entire job is fixing a tag-identity mistake, that is worth a
table. Added one, naming both repositories explicitly.

## The mechanism is now proven in BOTH directions

#3 showed only the failing invocation, which cannot establish that Go never falls back to a
root tag. An external control settles it — re-derived here in a purged module cache, and
pasted exactly as the terminal produced it:

```
$ go list -m github.com/hashicorp/consul/api@v1.32.0
github.com/hashicorp/consul/api v1.32.0
$ git ls-remote --tags https://github.com/hashicorp/consul refs/tags/v1.9.9 refs/tags/api/v1.32.0
1f486aab22986b70de1ae9aaa368d0613091898e	refs/tags/api/v1.32.0
8159a14bed92f774437618587fc8b38fe603ade1	refs/tags/v1.9.9
$ go list -m github.com/hashicorp/consul/api@v1.9.9
go: github.com/hashicorp/consul/api@v1.9.9: invalid version: unknown revision api/v1.9.9
$ git ls-remote --tags https://github.com/hashicorp/consul refs/tags/api/v1.9.9
(no output — the prefixed tag is absent)
```

The failing arm is not a case of "no such version anywhere": the ROOT tag `v1.9.9` genuinely
exists on that repo. What differs between the two arms is only whether the PREFIXED tag
(`api/<version>`) exists — present for `v1.32.0`, absent for `v1.9.9`.

The negative case is the decisive one: an existing root tag is still ignored for a
subdirectory module.

## The workflow prediction became an observation

#3 declined to add a `!v0.*` exclusion to the release trigger and said so plainly —
reasoned from glob semantics, never observed, with the first module tag named as the test.
That test has now run:

```
release.yml runs: 3    v2026.230.0708 (success), v2026.227.1233, v2026.227.0013
runs on a module tag:  0
```

Minting `candy/generate-packages/v0.2026230.708` triggered nothing. The guard would have
defended a non-problem. Section updated from "expected to" to the measurement.

## Smaller corrections

- **"Until a module tag exists"** read in tension with "both tags are minted at merge" now
  that one exists. Reworded to the case it actually describes: a consumer with no module tag
  to pin.
- **The `<HHMM>` zero-stripping rule** did not cover midnight. `0000` strips to `0`.
- **The sdk scheme** is cited with a link rather than by name alone.

## Deprecated runner

`actions/setup-go@v5` runs on Node 20, which GitHub annotates as deprecated on every run —
visible on the 2026-08-15 release run, so pre-existing drift rather than anything #3
introduced. Bumped to `@v7` (current latest) in `ci.yml` and `release.yml`.

`actions/checkout@v5` is bumped to `@v7` in the same edit, but **for currency only, not for
the deprecation**. An earlier draft of this entry and of commit `863b51e9` claimed checkout@v5
also ran on Node 20. **It does not** — its `action.yml` declares `using: node24`, and each
annotation names exactly one action, `actions/setup-go@v5`. The bump is harmless and the
version is current; the justification was appended to a true measurement without being
checked, which is precisely the defect class this PR exists to repair.

The bump is tested by this PR's `ci.yml` run — **`ci` event=`pull_request`, conclusion
`success`, zero annotations**, against **every prior `ci` and `release` run carrying the
deprecation annotation**.

**No count is given here deliberately.** An earlier draft said "all five prior runs", which
was a five-run SAMPLE transcribed as a total. Two independent exhaustive enumerations of all
20 runs then disagreed on the total (15 vs 16, differing on whether this branch's own runs
count as prior). A bare number in frozen history that two careful counts cannot reproduce is
a liability; the unqualified statement is true under both.

## The push trigger never fired for a working branch — fixed here, not routed

`ci.yml`'s trigger was `on: push: branches: ['*']`, and `*` does not match a `/`, so it never
fired for a `feat/…` branch — the same glob semantics this cutover relied on for the release
tag filter, applied in the other direction and missed. Measured: 3 push-event `ci` runs, all
on `main`; zero across the four `feat/*` branches pushed to this repo.

An earlier revision of this PR ROUTED that fix to "the next workflow touch in this repo". That
routing was vacuous — this PR *is* a workflow touch of that exact file, four lines above the
broken trigger — and it named a condition rather than a batch with an owner. Fixed here
instead: `'*'` → `'**'`. The fix is self-proving; a push-event `ci` run for this branch appears
in this PR's own run list.

**Correction to the commit message of `863b51e9`:** it also said `ci.yml` runs on push so the
branch push would test the bump. It did not, for the reason above. Neither claim can be amended
out of a pushed commit without a forbidden force-push, so both are corrected here.
