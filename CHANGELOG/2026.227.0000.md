# 2026.227.0000

Bump the sdk pin to `v0.2026227.1215` — the release carrying the
`packagekit` passphrase fix (`NFPM_*_PASSPHRASE` env expansion through
`buildOne`). The distro-repo workflows download the plugin from the latest
release, so this re-release is what makes the Phase 2 first publish sign
packages with the passphrase-protected distro-repo keys.

- **sdk pin**: `github.com/opencharly/sdk v0.2026226.1201` →
  `v0.2026227.1215` (`go mod tidy`).
- **No plugin code change**: the passphrase fix lives in `sdk/packagekit`;
  the plugin is the command surface only.
