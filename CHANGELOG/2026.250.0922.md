# 2026.250.0922 — chore(sdk): re-pin to sdk 758 / spec 709 (the packagekit systemd/config renderer)

## Summary

Re-pins the generate-packages command plugin to sdk v0.2026250.758 + spec v0.2026250.709 (from sdk v0.2026227.1215 / spec v0.2026225.1800). The distro package repos (charly-arch/fedora/ubuntu/debian) download this plugin's release binary at build time — the OLD pin has no packagekit systemd/config renderer, so the packages could not ship the charly-mcp units + /etc/charly/charly.yml. This pin bump carries the renderer from sdk #230 + #235 (the bare-project config fix).

## How tested

go build ./... exit 0. go test ./... (the e2e builds a real package per format + per variant):
```
ok  github.com/opencharly/plugin-generate-packages/candy/generate-packages  0.017s
```

## Rulebook compliance

- **R0:** git-workflow + the plan loaded.
- **R1:** RCA'd the gap live: the distro workflows download the plugin release (v2026.230.0800, sdk 227) which lacks the renderer.
- **R2:** the pin bump is the complete fix; the release.yml publishes the new binary on this tag.
- **R3:** one pin source (go.mod).
- **R4:** no workarounds — the re-pin is the real fix.
- **R4a/R5/R9/RDD/ADE/SDD/CleanArch/Kernel/Disposable/Hard-Cutover:** N/A or additive.
- **R6:** worktree, PR-only.
- **R7:** the e2e builds real packages (the renderer's output is covered by the sdk's render tests).
- **R8:** the e2e asserts the built package structure.
- **R10:** build + e2e green; the per-distro install-tests (the named batch) exercise the shipped units end-to-end.
- **AI Attribution:** footer below.

## Change classification

- **Change class:** code (dependency re-pin).
- **Verification gate:** go build + the package-building e2e.
- **Attribution tier:** fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*